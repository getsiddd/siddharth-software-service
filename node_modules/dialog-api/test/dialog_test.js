var Dialog = require("../lib/client");

describe("dialog", function() {
  var dialog = new Dialog("API_TOKEN", "botId")

  // describe("#track", function() {
  //   expect(dialog.track({})).to.equal("TODO");
  // });

  describe("#link", function() {
    it("returns a wrapped and encoded url", function() {
      expect(dialog.link("http://example.com", "id")).to.equal("https://api.dialoganalytics.com/v1/b/botId/clicks/?id=id&url=http%3A%2F%2Fexample.com");
    });
  });
});
